# Copy to .env and fill in with your Supabase project URL
# (Project Settings -> API -> Project URL, either cloud or local `pnpm db:status`).
#
# No secret needed: the auth guard verifies sessions against Supabase's public
# JWKS endpoint (derived from this URL), not a shared secret.
SUPABASE_URL=
