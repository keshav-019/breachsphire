# packages/ui

Shared React component library (HQ panels, mission cards, stat tiles, terminal
chrome) reused across `apps/web` and, later, `apps/admin`.

Not implemented yet — components currently live inline in `apps/web/src`
(`components/guardians` for domain components, `components/ui` for shadcn/ui
primitives) until enough of them repeat across apps to justify extraction.
