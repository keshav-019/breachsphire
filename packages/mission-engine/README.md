# packages/mission-engine

Runtime that interprets mission data (typed by `@cyber-guardians/types`) into
objective sequencing, challenge validation, completion conditions, and reward
grants. This is what lets new missions ship as data instead of code changes
(section 18).

Not implemented yet — Phase 2/3 of [`docs/09-development-phases.md`](../../docs/09-development-phases.md).
