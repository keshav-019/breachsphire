# packages/config

Shared lint/TS/Tailwind config presets for the monorepo, once enough apps
exist that duplicating config is more costly than centralizing it.

Not implemented yet — root `tsconfig.base.json` covers TypeScript for now.
